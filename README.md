# greynoise-ip-watch
A Bash script that resolves a dynamic DNS hostname to its current IP address, checks it against the GreyNoise Community API, and sends a push notification via ntfy.sh if the IP or its reputation status changes.
